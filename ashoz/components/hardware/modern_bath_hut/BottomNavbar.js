import classes from './BottomNavbar.module.css'


export default function BottomNavbar(props) {
    return (
        <div style={{backgroundColor: `#${props.bgColor}`}} className={classes.bottomnavbar}>
            
        </div>
    )
}
